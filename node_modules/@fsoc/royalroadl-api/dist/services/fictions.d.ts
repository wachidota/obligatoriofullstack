import { Requester } from '../requester.js';
import { RoyalResponse } from '../responses.js';
export interface FictionBlurb {
    id: number;
    type: string;
    title: string;
    image: string;
    tags: string[];
}
export interface NewReleaseBlurb extends FictionBlurb {
    description: string;
}
export interface LatestBlurb extends FictionBlurb {
    latest: {
        name: string;
        link: string;
        created: number;
    }[];
}
declare const statsKeys: readonly ["pages", "latest", "rating", "chapters", "followers"];
export interface PopularBlurb extends FictionBlurb {
    description: string;
    stats: Record<(typeof statsKeys)[number], number>;
}
export type BestBlurb = PopularBlurb;
export interface SearchBlurb {
    id: number;
    pages: number;
    title: string;
    image: string;
    description: string;
}
export declare class FictionsService {
    private readonly req;
    constructor(req: Requester);
    getLatest(page?: number): Promise<RoyalResponse<LatestBlurb[]>>;
    getPopular(page?: number): Promise<RoyalResponse<PopularBlurb[]>>;
    getBest(page?: number): Promise<RoyalResponse<PopularBlurb[]>>;
    getNewReleases(page?: number): Promise<RoyalResponse<NewReleaseBlurb[]>>;
    search(query: string, page?: number): Promise<RoyalResponse<SearchBlurb[]>>;
    getList(type: string, page?: number): Promise<string>;
}
export {};
