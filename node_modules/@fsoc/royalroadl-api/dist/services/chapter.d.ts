import logger from 'debug';
import { Requester } from '../requester.js';
import { RoyalResponse } from '../responses.js';
export interface NewChapter {
    title: string;
    content: string;
    preNote?: undefined | string;
    postNote?: undefined | string;
}
export interface Chapter {
    content: string;
    preNote: string;
    postNote: string;
    next: number;
    previous: number;
}
export interface ChapterComment {
    id: number;
    posted: number;
    content: string;
    author: {
        id: number;
        name: string;
        avatar: string;
    };
}
export declare class ChapterService {
    private static isValidNewChapter;
    debug: logger.IDebugger;
    private readonly req;
    constructor(req: Requester);
    publish(fictionID: number, chapter: NewChapter): Promise<RoyalResponse<NewChapter>>;
    getChapter(chapterID: number): Promise<RoyalResponse<Chapter>>;
    getComments(chapterID: number, page?: number | 'last'): Promise<RoyalResponse<ChapterComment[]>>;
    postComment(chapterID: number, content: string): Promise<RoyalResponse<null>>;
    private requireAuth;
}
