import { Requester } from '../requester.js';
import { RoyalResponse } from '../responses.js';
export interface MyFiction {
    id: number;
    title: string;
}
export interface Notification {
    link: string;
    image: string;
    message: string;
    timestamp: number;
}
export interface Bookmark {
    id: number;
    title: string;
    image: string;
    link: string;
    author: {
        id: number;
        name: string;
    };
}
export interface ReadLater {
    id: number;
    title: string;
    image: string;
    link: string;
    pages: number;
    author: {
        id: number;
        name: string;
    };
}
export declare class UserService {
    private readonly req;
    constructor(req: Requester);
    get isLoggedIn(): boolean;
    login(email: string, password: string): Promise<RoyalResponse<string>>;
    getMyFictions(): Promise<RoyalResponse<MyFiction[]>>;
    getMyFollows(page?: number): Promise<RoyalResponse<Bookmark[]>>;
    getNotifications(): Promise<RoyalResponse<Notification[]>>;
    getMyReadLater(page?: number): Promise<RoyalResponse<ReadLater[]>>;
}
